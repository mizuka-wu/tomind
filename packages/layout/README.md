# @tomind/layout

布局引擎：树形、矩阵、鱼骨等多种布局算法。

## 导出

- `layout()` / `layoutTree()` — 执行布局计算
- `registerLayout()` / `unregisterLayout()` — 注册/注销布局算法
- `DEFAULT_LAYOUT_OPTIONS` — 默认布局参数
- `matrixLayoutAlgorithm` — 矩阵布局
- `Matrix` / `MatrixCell` / `MatrixContainer` — 矩阵数据结构

## 依赖

- `@tomind/schema`
