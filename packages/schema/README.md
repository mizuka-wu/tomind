# @tomind/schema

NodeDesc、SelectionState、Viewport 等核心类型定义。

## 导出

- `NodeDesc` — 节点描述（id, type, attrs, children）
- `NodeInfo` / `NodeRole` — 节点信息与角色
- `SelectionState` — 选区状态
- `Viewport` — 视口状态
- `createNode()` — 节点工厂函数
- `ATTRIBUTE_TITLE` — 标题属性常量

## 依赖

无外部依赖。
