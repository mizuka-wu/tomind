# @tomind/commands

CommandDef + CommandManager 命令系统。

## 导出

- `CommandDef` — 命令定义
- `CommandManager` — 命令管理器（execute, register, canExecute）
- `NodeCommands` / `ClassCommands` — 节点命令与类命令

## 依赖

- `@tomind/schema`
- `@tomind/state`
